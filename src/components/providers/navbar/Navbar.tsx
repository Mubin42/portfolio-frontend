import { FC } from "react";

type Props = {};

const Navbar: FC<Props> = ({}) => {
  return (
    <div className="flex w-full h-16 bg-navbar">
      This is the navbar component
    </div>
  );
};

export default Navbar;
